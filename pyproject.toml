[project]
name = "jsanjuanjover-site"
version = "0.1.0"
description = "Personal website, CV and blog — Quarto sources"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1",
    "pandas>=2.2",
    "matplotlib>=3.9",
]
